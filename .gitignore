# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
*.egg
.eggs/

# Virtual environments
.venv/
venv/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Environment
.env
.env.local

# Model weights — large, never committed; pulled from HF as needed
models/
*.safetensors
*.bin
*.gguf
*.pt
*.pth
*.onnx

# Data artifacts live on HuggingFace, pulled at reproduce time, never committed
data/canonical_direction.pt

# Copyrighted canonical text translations (NIV, ESV, modern Yusuf Ali, etc.)
# Pulled at experiment-run time by scripts/fetch_external_prompts.py per
# CLAUDE.md "Canonical text prompts" rule. PD translations (KJV, JPS 1917,
# Pickthall 1930, Arnold 1885, Legge, Müller, Book of Mormon 1830) go in
# data/prompts/ directly and ARE committed.
data/prompts/external/

# HF dataset / activation caches
.cache/
cache/
activations/

# Derivation outputs — the .pt direction tensors and logs
# (the convergence_analysis.md files IS committed; everything else regenerates)
results/**/directions/
results/**/*.log
results/*.log
